const host = "http://localhost:5000"
export const registerRoute = `${host}/api/auth/register`; // path to the login endpoint of your server

export const loginRoute = `${host}/api/auth/login`; // path to the login endpoint of your server

export const setAvatarRoute = `${host}/api/auth/setAvatar`; // path to the login endpoint of your server



